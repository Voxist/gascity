[order]
description = "Check beads provider health; recovery is left to the managed Dolt lifecycle owner (the controller)"
trigger = "cooldown"
interval = "30s"
exec = "gc beads health --quiet"
timeout = "60s"
reserved_dispatch = true
